<!DOCTYPE html>
<html lang="en">


<head>
    <meta charset="utf-8">
    <title>{{title}}</title>
    <link rel="stylesheet" href="/public/css/main.css">
</head>

<body>
    <nav>
        
        {{#if login}}
        <p>current status: login</p>
        <a href="/user/{{user.id}}">{{user.fullName}}</a><br></br>
        <a href="/logout">Click to logout</a>
        {{else}}
        <p>current status: not login</p>
        <li>
            <a href="/login">Login</a>
        </li>
        <li>
            <a href="/register">Sign Up</a>
        </li>
        {{/if}}

        <li>
            <a href="/">Homepage</a>
        </li>
    </nav>
    {{{body}}}
</body>

</html>